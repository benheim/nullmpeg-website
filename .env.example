# Sentry client-side error tracking (recommended)
VITE_SENTRY_DSN=

# Deployment metadata tags
VITE_APP_ENV=production
VITE_APP_RELEASE=local-dev

# Optional fallback endpoint for custom ingestion (still supported)
# VITE_CLIENT_ERROR_ENDPOINT=/api/client-errors
